# 프로그램 워크플로우 (Workflow)

이 문서는 '당뇨 혈당 관리 매니저 PRO'의 내부 동작 로직을 설명합니다.

## 1. 초기 시스템 가동
1. **브라우저 접속**: 사용자가 URL에 접속합니다.
2. **기기 ID 확인/생성**: 
   - 브라우저의 `localStorage`에서 `sugarApp_deviceId`를 확인합니다.
   - 없을 경우, 고유한 랜덤 문자열(기기 ID)을 생성하여 저장합니다.
3. **백엔드 마이그레이션**:
   - 기존에 로컬 저장소(`localStorage`)를 사용하던 데이터가 있다면, 현재 생성된 기기 ID를 부여하여 서버(`db.json`)로 일괄 전송합니다.

## 2. 데이터 조회 (Read)
1. **API 요청**: `GET /sugarData?deviceId={내기기ID}` 명령어를 통해 서버에 데이터를 요청합니다.
2. **필터링**: 서버는 전체 데이터 중 해당 기기 ID와 일치하는 데이터만 반환합니다.
3. **화면 렌더링**:
   - 요약 대시보드(평균, 최고, 최저) 업데이트
   - 당화혈색소(HbA1c) 예측치 계산
   - 최근 추이 차트(Chart.js) 출력
   - 히스토리 목록 출력

## 3. 데이터 기록 (Create)
1. **입력**: 사용자가 이름, 혈당치, 측정 종류(공복/식후) 등을 입력합니다.
2. **데이터 구성**: 입력값에 `deviceId`를 포함한 JSON 객체를 생성합니다.
3. **API 요청**: `POST /sugarData`를 통해 서버에 저장합니다.
4. **갱신**: 저장 성공 시 화면을 다시 로드하여 최신 목록을 보여줍니다.

## 4. 데이터 삭제 및 관리
1. **삭제**: 특정 기록의 `id`를 기반으로 `DELETE /sugarData/{id}` 요청을 보냅니다.
2. **백업/복구**: 
   - **내보내기**: 현재 기기의 데이터를 JSON 또는 CSV 파일로 다운로드합니다.
   - **가져오기**: 외부 JSON 파일을 읽어 현재 기기 ID를 새로 부여한 후 서버에 업로드합니다.
